apiVersion: v1
kind: Service
metadata:
  name: library-frontend
  namespace: library-management
spec:
  type: NodePort
  selector:
    app: library-frontend
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30081